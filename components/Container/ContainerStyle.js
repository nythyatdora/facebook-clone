import { StyleSheet } from 'react-native';
import * as Constraint from '../MainStyle';

export const container = StyleSheet.create({
    default: {
        flex: 1,
        backgroundColor: Constraint.secondaryColor,
    }
});